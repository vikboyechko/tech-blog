// byron suggestion to put this in its own file
// not using this at the moment

// function isAuthenticatedUser(req, res, next) {
//     return !req.session.loggedIn ? res.redirect('/login') : next();
// }

// module.exports = isAuthenticatedUser;
